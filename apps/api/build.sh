#!/usr/bin/env bash
# Build script is handled by buildpack (pip install & collectstatic are automatic).
# We skip manual commands here to avoid environment path issues.
echo "Build phase completed by buildpack."
